# 小熊电器客户原声分析与产品改进挖掘 · 状态板（给 AI · 跨会话唯一接续点）

> 开工先读 `CLAUDE.md` + **`.42cog/` 四份**（`intent` · `real` · `cog` · `meta`）+ 本文件 + `state/memory/MEMORY.md`。
> **非轮规则：每轮有效工作必更新本文件**（倒序追加，新的在上，带日期与 commit hash）。
> 每完成一个可命名的逻辑单元存一次；破坏性操作之前也存一次。这是给你自己留的后路，不是给别人看的历史。

> ## 🌱 系统开仓（2026-08-15 第一轮）
> `aias-meta-init` 生成六组骨架：① `README.md` `CLAUDE.md` `.42cog/` `specs/` ② `vault/` `notes/` `resources/` ③ `skills/` `scripts/` `plugin.json` ④ `reports/` ⑤ `state/` `docs/` ⑥ `_build/` `_tmp/` `_archive/`（忽略）。
> **收敛方向**（草稿，待人确认）：见 `.42cog/intent.md`——**那句话只有一份，别抄到这里来**。
> **下一步**：确认收敛方向 → `xbd-research` 找依据、排真相源权重 → 回来改这一句。方向第一版粗是正常的，四步是循环。

> ## 人定的两件事（2026-08-15）
> **① 方向放宽**：不止「改现有产品」，**新增新品概念出口**——原声里反复冒头、现有产品线接不住的未满足需求也要挖出来。
> 意向书据此重写：判据分改进型 / 新品型两栏，真难题加第三条（分不出个人怪癖与一群人的真需求），新品护栏 = ≥5 条独立原声。详见 `.42cog/intent.md`。
> **② 放权边界**：读取分析写报告 · 装工具装依赖 · git 提交 → 放行；推送 / 不可逆动作 / 业务实质 → 先问。已写进 `CLAUDE.md` 第二节（唯一正本）。

## 待办

- [ ] **配远程仓库**（欠着的第一件事）。本地 `.git` 已建，但没有 remote；`.gitignore` 忽略的那几个目录**没有任何远端副本，本地一丢就没了**。
      **人已答应给地址，等地址**。拿到后：`git remote add origin <url>`。⚠️ 客户原声含个人信息，**必须建私有仓**
- [x] 补装工具：`rg` `gitleaks` `gh` `bun` `uv` 已装（2026-08-15，brew，均验证过版本）
- [ ] 对抗性评审要用的**第二谱系**（`codex` / `opencode`）一个都还没有 —— 少一双不同来路的眼睛，见 `bash scripts/check-tools.sh`
- [ ] `.42cog/real.md` `cog.md` `meta.md` 仍是模板占位，待 `xbd-research` 那一步填实
- [ ] `vault/raw/` 是空的 —— 第一批客户原声（退货记录 / 追评 / 客服工单导出）还没进来。**脱敏后才入库**
- [ ] 冒烟测试没做：新开一个空会话，只喂 `.42cog/intent.md`，问三句（朝哪使劲 / 不做什么 / 下一步干什么）。答不齐说明方向没写清
