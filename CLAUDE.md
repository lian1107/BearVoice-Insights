# CLAUDE.md — 小熊电器客户原声分析与产品改进挖掘（xbd）

> AI 开工必读：本文件 + **`.42cog/` 四份**（`intent` 意向书 · `real` 现实约束 · `cog` 认知模型 · `meta` 项目身份）
> + `state/board.md` + `state/memory/MEMORY.md`。

## 一、这个系统朝哪个方向使劲

**见 `.42cog/intent.md` 最上面那一句。** 那是唯一正本，冲突时以它为准；
真难题 · 不做什么 · 真相源权重 · 作品怎么算合格，都在同一份文件里。

> **这里为什么不抄一份过来**：方向会改，抄出来的那份改不到，就成了错的。
> 同一句话只留一处——这是铁律七「唯一编码」的第一个例子，你每天都会用到它。

> ⚠️ **这一节永远只留路径，不留内容。** 不重复、不摘要、不举例——
> 你顺手抄一句过来，它下周就成了错的那一份。

> 作品 = `reports/` 下的单元。

> **分工线**：`.42cog/` 全是**陈述句**——这个系统是什么样；本文件全是**祈使句**——你该怎么干；
> `specs/` 是**标准**——产出该长什么样（含命名）。**三者不许互相串门。**

## 二、开工协议

> 开工前先做确定性分流：确定的你来执行，需要我拍板的，请你在充分调研后，列出来等我定。动手前先冒烟，确认上下文够、权限够；方向尚未确定的，先跑一次有界小实验。

顺序即：**先分流，再冒烟，必要时补一次有界小实验。**

**任务编排三法**（写进这里就每次自动生效）：**合并同类项**——手上压着好几件事时先分组，
同类同批处理；**确定性分流**——见上；**开工前先同步**——别基于陈旧的本地状态动手。

**意外矩阵**：提前写好「如果 X，那么 Y」。落点只有一个——**永不停摆**。
单条卡住跳下一条；**同一类错误犯到第三次，必须沉淀成一条规则**，写进本文件或 `state/memory/`。

**护栏**：破坏性命令（批量删除、强制覆盖、强推）先拦下来。巧处在于**拦得住，但不堵死**——
确需执行的，加一个显式前缀重发即放行，并留下痕迹。「文件还在」和「工作还在」，是两回事。

### 放权边界（2026-08-15 人定，唯一正本）

**决策前置解决「哪些事该他定」，这一节解决「定完之后放多长的绳」。**

| 动作 | 放行 | 附带条件 |
|---|---|---|
| **读取 · 分析 · 写报告** | ✅ 直接做 | 读数据、跑分析、往 `reports/` `docs/` `state/` `_tmp/` 写文件，不必逐条问 |
| **装工具 · 装依赖** | ✅ 直接做 | 只从官方或可信源；装前核一眼包名与维护状态；装完记一笔 |
| **git 提交** | ✅ 直接做 | 每个可命名的逻辑单元提交一次，按第七节标注提交链 |
| **git 推送** | ❌ 先问 | 推送是对外动作，一旦推出去就收不回 |
| **删除 · 覆盖 · 批量改名 · 对外发布** | ❌ 先问 | 不可逆，一律先确认（铁律 12） |
| **业务实质**：产品结论、给客户/供应商的东西、定价、对外口径 | ❌ 先问 | 技术怎么做是你的事，**业务上算不算数是人的事** |

> 放行不等于免报告：**每步先说要干什么，做完简要汇报。** 放的是「不用等我点头」，不是「不用告诉我」。

## 三、目录：六组

**一到五是我的，第二组是别人的，第六组是垃圾。**

### 1　开工手册与规约 —— 我立的规矩

| 位置 | 装什么 |
|---|---|
| `README.md` | **给人看的门面**：这个系统是什么、从哪儿开始、目录一眼看懂 |
| `CLAUDE.md` | 本文件。开工协议与分流规则，AI 每次开工先读 |
| `.42cog/` | **这个系统是什么**（四份陈述，不含规矩）：`intent` 意向书（冲突时以它为准）· `real` 现实约束 · `cog` 认知模型 · `meta` 项目身份 |
| `specs/` | 产出该长什么样：风格（正例与负例）· 格式 · 命名 |

> **手册管怎么干，规约管产出该长什么样。**

### 2　真相源与参考 —— 别人的材料

| 位置 | 谁的 | 能引到什么程度 |
|---|---|---|
| `vault/raw/` | 外部与历史原件 | **只读**，不改不删不重排 |
| `vault/truth/` | 整理后的真相源 | **可引用**，冲突时信它；带出处 |
| `notes/` | 内部同事的笔记 | **只读参考**，不改、不代提交 |
| `resources/` | 外部第三方 | **只读参考，读懂后自己写，绝不 copy-paste** |

> 这一组只有一条线要记：**能引用到什么程度。** 动了 `notes/` 是冒犯，抄了 `resources/` 是侵权。

### 3　脚本与扩展 —— 我的手段

| 位置 | 装什么 |
|---|---|
| `skills/` | 本系统技能，按四步走命名：`xbd-init` / `-research` / `-mini` / `-evolve` |
| `scripts/` | 项目专属命令。**不放全局 `bin/`**——对整台电脑可见，容易过载，安全风险也高 |
| `plugin.json` | **我是谁**：本系统作为插件包的身份（名字/版本/许可），跨厂商格式 |
| `42plugin.json` | **我装了谁**：从哪装、装了哪些外部扩展。这一层标准未定，随插件工具而变 |
| `.claude/workflows/` | **动态编排**：路线不是预设的，是当场写出来的。已带一份多视角自检 |

> 两份清单并存不是冗余：**一个 AI 自主系统，既装别人的能力，也把自己的能力打成包给别人装。**
> 改完任一份跑 `python3 scripts/check-manifests.py`——名字里多一个连字符、首字符用了数字，
> 肉眼扫不出来，装的时候才报错。**这就是三层验证的第一层：能用规则判的，绝不留给人判。**


> 能用规则判的，一律先写成脚本。核心功能先做成命令行——它给出文字反馈，AI 才能自判对错、自迭代。

### 4　作品 —— 我产的东西

| 位置 | 装什么 |
|---|---|
| `reports/` | 这个系统真正生产的东西。创作型单元人机分文件：`hi.md` 人类手稿（AI 只读）+ `readme.md` AI 工作台 |

### 5　状态与文档 —— 我的账

**状态外部化**：把该记的搬出对话、落进文件——对话窗口活不过这一轮，文件活得过。
按「这份材料要活多久」分：

| 位置 | 装什么 |
|---|---|
| `state/board.md` | 状态板：这一轮干到哪了。每完成一个可命名的逻辑单元存一次，破坏性操作前也存一次。**是给自己留的后路，不是给别人看的历史** |
| `state/memory/` | 记忆库：记教训，下次自动生效 |
| `state/changelog.md` | 变更档案：这一轮为什么这么定、否决了哪些方案 |
| `docs/plan/` | 决策点与拍板记录 |
| `docs/experiments/` | 有界小实验：`exp001-<slug>/`，让方案由数据定 |
| `docs/research/` `docs/reviews/` | 调研结论 · 对抗性评审 |
| 提交链 | 版本历史的真身。文件名恒定，历史在提交里 |

### 6　过程材料 —— 扔的

`_build/`（构建产物，可重建，只许 `YYYYMMDD-HHMMSS/`）· `_tmp/`（一次性过程材料，只许 `YYYYMMDD-slug/`）· `_archive/`（快照，只许 `YYYYMMDD-slug/`）

> 共性不是「临时」，是**都不入库**。三区日期一律打头方能一路排序；均禁裸文件与目录混住。

## 四、装不上怎么办：三层降级

工具缺失时按这三层走，**一层不成才进下一层**：

| 层 | 干什么 | 在哪 |
|---|---|---|
| 一 · **用哪个** | 本项目该用哪些 CLI，已经定死了。**别自己挑，别现查** | `scripts/check-tools.sh` |
| 二 · **装不上怎么办** | 同生态换另一个入口（`npm`↔`bun`、`pip`↔`uv`）；还不行换一条不依赖它的做法，并记一笔 | 本节 |
| 三 · **去哪装** | 先看本地已有什么，再走镜像，**高校源优先** | `check-tools.sh --mirrors` |

> **不许在第一层就开始换源、换写法、反复试。** 换个写法再试一次、再换一个源、再试一次——
> 时间和 token 就是这样烧掉的。三层走完仍然不成，**停下来告诉我**，别自己绕。

两条底线：**只从官方或可信源装**；装之前核一眼包名与维护状态。

## 五、并行怎么开，什么时候收

**开几条**：worktree（**工作副本**）是 Git 官方机制，不属于任何一家 AI 厂商，换工具带得走。
一个分身占一个副本，**各占一份完整硬盘空间**——先看盘还剩多少再决定开几条。
**别套娃**：worktree 里再开 worktree，路径越缠越乱。退出时留意那句提示，
**有未提交的改动它会问你留不留，那一问别手快点掉**。

**怎么审**：自检不是再读一遍——再读一遍，AI 只会再一次确认自己是对的。
有效的自检是**几个互相对抗的视角同时审**，视角越分化越管用。现成的在 `.claude/workflows/`。

**换谱系审**：同一个会话里再问一遍没用——偏见有两个来源，上下文污染和模型权重。
新开会话只洗掉前一半。**要换就换整套装置**：第三方的框架配第三方的模型。
装了哪些见 `scripts/check-tools.sh`。

```
codex exec -m <模型> -c model_reasoning_effort=medium < 提示词文件
cat 提示词文件 | opencode run --model <厂商/模型>
```

> ⚠️ **提示词绝不直接写进命令行，一律先落成文件再让命令读它。**
> 两个原因：提示词里可能带危险命令，拼进 shell 有被执行的风险；各家对转义符处理不一致，
> 特殊字符会被吃掉，它根本没读懂你要什么。**「读文件」要真的读**——
> 用文件参数或标准输入，别用会先把内容展开再塞回命令的写法。
>
> ⚠️ **发出去之前过一道**：这份材料有没有权限外发？密钥、个人信息、未公开的内容摘掉了吗？

**什么时候收**：按 P0–P3 定级，**P0 / P1 / P2 修复为零，P3 酌情**。
到这条线就停，不再开下一轮——**修复原则定好了，无限评审才有出口**。

## 六、三层验证：能用规则判的，绝不留给人判

| 层 | 谁判 | 落在哪 |
|---|---|---|
| 规则验证 · 判对错 | 机器，瞬时近免费 | `scripts/` 下的脚本 |
| 判据验证 · 判好坏 | AI 按写下的判据代判 | 技能内置判据 · `specs/` 里的正例与负例 |
| 实效验证 · 判成败 | 人与世界一起判 | 定标与验收——四步里唯一不能交给 AI 的一步 |

判不了的那一截就是**验证红线**，留给人。

## 七、提交链标注（五行，照抄）

```
Change-By:      human | ai | ai+human
Agent:          用的哪个工具
Model:          哪个模型
Suggested-By:   建议来自谁
Co-authored-by: 姓名 <邮箱>
```

**作者亲手改的那些提交，就是日后喂给 AI 学你风格的唯一真相源。**

## 八、铁律

1. **可以写错，不可以空着**：收敛方向必须有一句。空着的方向会被平均值填满。
2. **`hi.md` 神圣**：人类自由手稿，AI 只读，绝不修改、重排、格式化、加提示语。
3. **`vault/raw/` 只读**；`notes/` 不改不代提交；`resources/` 读懂后自己写，绝不 copy-paste。
4. **人手为界**：经人手的产物 = 原件，不可弃；未经人手的试错才可丢。
5. **旧实践迁就新理念**：旧产物是**事实**的真相源，不是**标准**的真相源。冲突时改旧产物。
6. **输出超过三百 token，就写入文件**——三百 token 大约两三百个汉字，一段像样的分析、一份小结、
   一张清单都超过这个量。**对话里只留一个路径和一句摘要，正文进文件。**
   按寿命分流：一次性→`_tmp/`；状态→`state/board.md`；经验→`state/memory/`；需追溯→`docs/NNN_`。
7. **唯一编码**：每份文档一个独一无二的名字，同样的内容只留一份，**不留副本**——有两份就已经失真，还得额外维护一张对账表。
   **引用已有内容时只给路径与行号，不要把全文再贴一遍**（既不产生第二份真相，也少一条密钥泄露路径；
   但真要护住密钥靠的是权限，不是习惯）。版本走提交链：不留 `v2`、`-final` 这类文件名；
   改动前先提交；**绝不用 `cp` 覆盖被改过的文件**。
   所有文档按 `specs/` 的三种命名法命名：档案用流水编号、过程材料用创建日期、记忆用最后更新日期。
8. **大文件不进版本库**：单文件**超过 10 MB 先想清楚，超过 50 MB 一律不进**——
   AI 一天提交上百次很常见，一个大文件每改一次就要完整存一遍，硬盘立刻吃不消。
   走大文件扩展（`git-lfs` 之类，按后缀名指定哪些走它），或放 NAS / 云盘。
   **仓里只留三样：路径 · 哈希 · 一句说明**（哪台机器、什么时候放的）。
   算哈希：`shasum -a 256 <文件>`。这就是指针化引用用到文件这一级——**不搬运内容，就不会产生第二份真相**。
   ⚠️ 放在仓外就**没有远端副本**，删之前先确认另一处还在。
   厚材料（几十上百页的资料）同理：先整理一份索引，平时只带索引，要细节再回原件。
9. **每轮必更 board**；里程碑蒸馏进 changelog（只答：变了什么 / 为什么变 / 对你意味着什么）。
10. **新顶层目录 = 协议事件**：先答两问（作品怎么验证？真相源是谁？），记 changelog，人拍板。
11. **反意图污染**：整理人的口头指令时别扩写稀释——十个强信号扩成千字会稀释三倍。保护最强意图。
12. **安全**：密钥进 `.env.local` 永不入库；含个人隐私的数据不入库；删除、对外发布、批量改名等不可逆动作前先确认。
13. **提交与推送按第二节「放权边界」那张表走**——提交放行，**推送先问**。
    （这里不重复那张表：绳放多长会变，抄一份出来就是日后错的那一份。）

## 九、约定

沟通与文档用中文，文件名用英文 ｜ 单次回复尽量短，长内容写文件后给路径 + 一句摘要 ｜ 提交前缀 `feat/fix/docs/chore`

---

*由 `aias-meta-init` 生成于 2026-08-15。这份文件会越写越细——先粗后细是正常的。*
