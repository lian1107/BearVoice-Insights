# ── 第六组 过程材料：都不入库。日期一律打头方能一路排序；均禁裸文件 ──
# 构建产物（可重建）：只许 YYYYMMDD-HHMMSS/，一次构建一个——按天分会撞车
_build/
# 一次性过程材料（用完即弃）：只许 YYYYMMDD-slug/
_tmp/
# 快照：只许 YYYYMMDD-slug/
_archive/*
# 例外：需入库可追溯的下线件，目录名以 -delisted 结尾，带 README 说明为什么下线
!_archive/*-delisted/

# Git 隔离工作区：本地开发副本，不进入主仓库
.worktrees/

# ── 第二组 外部参考：本地留存，仅 README 入库 ──
resources/**
!resources/README.md

# ── vault/raw/：脱敏后的数据直接入库（私有仓，git 就是备份和版本历史）──
# 唯一前提是**先脱敏**。没脱敏的原始导出放 _tmp/，别搁 vault/raw/。
# 下面两条只是防手滑。
vault/raw/**/未脱敏/
vault/raw/**/*.原始.*

# 注：notes/ 装同事写的、要读的东西，**入库**。只有你自己看的速记不放这里，放 _tmp/

# 大件（本地 + 云盘，绝不入库；按需加例外）
*.key
*.pptx
*.pdf
*.mp4
*.mov
*.zip

# 密钥（永不入库）
.env
.env.local
.env.*.local

# 42plugin 装进来的技能：是指向本机 ~/.42plugin/cache 的绝对路径软链接，
# 入库换台机器就是死链。装了哪些记在 42plugin.json 里，`42plugin install` 一条命令重装回来。
# 注：.claude/workflows/ 是本项目自己的文件，照常入库。
.claude/skills/

# 跨会话进度档：由本机 hook 原子更新并签名（progress-protocol:v1），是会话状态不是项目产物
.claude/progress.md

# 系统
.DS_Store
__pycache__/
*.pyc
node_modules/
.venv/
.pytest_cache/
platform/.data/
platform/frontend/dist/
platform/frontend/playwright-report/
platform/frontend/test-results/
